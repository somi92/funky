var Flow = (function () {

    var id = function (obj) {
        return obj;
    };

    var tap = function () {

    };

    var alt = function () {

    };

    var seq = function () {

    };

    var fork = function () {

    };

    return {
        id: id,
        tap: tap,
        alt: alt,
        seq: seq,
        fork: fork
    }

})();

module.exports = Flow;